<h2>New Book</h2>
<form action="/books/<%= page == 'edit' ? `${book.id}?_method=PUT` : "" %>" method="POST">
    <%- include('_form_fields') %>
        <a href="/books">Cancel</a>
        <button type="submit">Create</button>
</form>